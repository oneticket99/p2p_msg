# SPDX-License-Identifier: GPL-3.0-or-later
# TooTalk(p2p_msg) — app/ui/ 의 pytest 단위 테스트 패키지
#
# 본 디렉토리 의 test 의 의 의 PyQt6 의 의 의 의 staticmethod / 순수 함수 만 우선 커버.
# QWidget 통합 (signal / slot / repaint) = pytest-qt 별도 cycle 위탁.
