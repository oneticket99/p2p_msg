# TooTalk(p2p_msg) Phase 1 MVP 시그널링 서버 의존성
# Python 3.13 기준. 의존성 최소화 — aiohttp + dotenv + DB + SMTP.

aiohttp>=3.11
python-dotenv>=1.0
# MariaDB 영속화 (사용자 directive 2026-05-17 — Phase 1 영속화 DB)
asyncmy>=0.2.10
# 이메일 OTP 발송 (사용자 directive 2026-05-17 — postfix 데모 서버 의 SMTP client)
aiosmtplib>=3.0
# Phase 3 bot framework LLM provider HTTP transport (사용자 directive 2026-05-22)
# AnthropicClient + OpenAIClient httpx_transport factory 의 실 binding.
# httpx 미설치 시 AnthropicProvider.is_available() / OpenAIProvider.is_available()
# 모두 False 반환 (graceful) → MockLLMProvider 폴백. 프로덕션 배포 의무.
httpx>=0.27
# Phase 4 cycle 103~104 — Firebase Cloud Messaging API v1 SDK
# app/notifications/fcm_client.py 의 실 binding. 미설치 시 graceful
# log warning + push skip (PULL 폴백). production 의무.
firebase-admin>=7.0
# Phase 3 cycle 132~151 — bot streaming (chzzk/twitch/kick WebSocket chat 구독)
# app/bot/streaming/*.py + app/bot/obs_websocket_client.py 의 실 binding.
# 미설치 시 graceful — provider.is_available() 반환 False + mock 폴백.
websockets>=12.0
# Phase 3 cycle 138~145 — emoji 이미지 moderation (DMCA pHash + OCR)
# app/bot/emoji_dmca_check.py + app/bot/jailbreak_detector_ocr.py 의 실 binding.
# 미설치 시 graceful — check_dmca() / detect_ocr() 가 empty 반환 + log warning.
# 본 3 dependency = optional + 데모 서버 의무 (production emoji 검수 활성 시).
Pillow>=10.0
pytesseract>=0.3.10
ImageHash>=4.3
# Phase 4 cycle 169.799 — SFU(Selective Forwarding Unit) 그룹 통화 미디어 서버.
# server/sfu_room.py 의 RTCPeerConnection + MediaRelay 실 binding (9+ peer 그룹 음성·영상 fan-out).
# python:3.13-slim 에서 av/pylibsrtp manylinux wheel 이 native lib 자체 포함 →
# 추가 시스템 라이브러리·빌드 의존 불요 (cycle 169.828 throwaway 컨테이너 빌드+import 검증).
# 미설치 시 sfu_room.AIORTC_AVAILABLE=False → SFU 비활성 + 코어 시그널링/인증/메시지 graceful 부팅 유지.
aiortc>=1.14
