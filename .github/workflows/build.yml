# SPDX-License-Identifier: GPL-3.0-or-later
# TooTalk(p2p_msg) Phase 1 build workflow — macOS native + Windows wine cross-compile.

name: build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: 임시 빌드 태그
        required: false
        default: "manual"

jobs:
  build-macos:
    name: macOS arm64 (.app)
    runs-on: [self-hosted, macOS, ARM64]
    steps:
      - uses: actions/checkout@v4
      - name: Python 3.13 venv + dev deps
        run: |
          python3.13 -m venv .venv
          .venv/bin/pip install --upgrade pip
          .venv/bin/pip install -r app/requirements.txt pyinstaller>=6.0
      - name: PyInstaller build
        run: .venv/bin/python tools/build.py --target macos
      - name: Zip artifact
        run: |
          cd dist && zip -r "TooTalk-macos-arm64.zip" "TooTalk.app"
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tootalk-macos-arm64
          path: dist/TooTalk-macos-arm64.zip
          retention-days: 30

  build-windows-wine:
    name: Windows x64 via wine (.exe)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: PyInstaller wine cross-compile
        run: |
          # cdrx/pyinstaller-windows:python3 default entrypoint = /entrypoint.sh — CMD args 무시 → --entrypoint /bin/sh override 의무 (cycle 132 회수)
          docker run --rm \
            --entrypoint /bin/sh \
            -v "$PWD:/src" \
            -w /src \
            cdrx/pyinstaller-windows:python3 \
            -c "pip install -r app/requirements.txt && pyinstaller build/tootalk.spec --clean --noconfirm --distpath dist-windows --workpath build/work-win && ls -la dist-windows"
      - name: Zip artifact
        run: |
          # PyInstaller 산출 디렉토리 명 정합 (TooTalk 또는 TooTalk.exe 단일 파일) — ls 로 확인 후 zip
          ls -la dist-windows || (echo "dist-windows 부재 — PyInstaller 산출 실패" && exit 1)
          cd dist-windows && zip -r "TooTalk-windows-x64.zip" .
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tootalk-windows-x64
          path: dist-windows/TooTalk-windows-x64.zip
          retention-days: 30
