# 클라이언트 개발 의존성 — pytest + coverage + lint
# 사용자 directive 2026-05-17 — "qa 단계에서는 pytest 당연히 필요해"
# 본 파일은 .env 격리 외 — 모든 contributor 측 동일 버전 사용

-r requirements.txt

pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1
pytest-qt>=4.4
pytest-mock>=3.12
# Playwright E2E (DESIGN.md §10.6 — 사용자 directive 2026-05-17)
pytest-playwright>=0.5
playwright>=1.42
